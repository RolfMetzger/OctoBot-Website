{% extends 'base.html.twig' %}

{% block title %}PackageCategory{% endblock %}

{% block body %}
    <h1>Package Type</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ package_category.id }}</td>
            </tr>
            <tr>
                <th>Short name</th>
                <td>{{ package_category.shortname }}</td>
            </tr>
            <tr>
                <th>Long name</th>
                <td>{{ package_category.longname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('package_category_index') }}">back to list</a>

    <a href="{{ path('package_category_edit', {'id': package_category.id}) }}">edit</a>

    {{ include('package_category/_delete_form.html.twig') }}
{% endblock %}
