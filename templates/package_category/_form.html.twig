{{ form_start(form) }}

    {{ form_widget(form) }}

    <a class="btn btn-secondary" href="{{ path('package_category_index') }}"><i class="fas fa-angle-left"></i> Back to list</a>
    <button class="btn btn-primary"><i class="fas fa-check"></i> {{ button_label|default('Save') }}</button>

{{ form_end(form) }}
