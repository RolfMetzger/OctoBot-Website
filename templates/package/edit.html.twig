{% extends 'base.html.twig' %}

{% block title %}Edit Package{% endblock %}

{% block body %}
    <h1>Edit Package</h1>

    {{ include('package/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" class="btn btn-primary" href="{{ path('package_index') }}"><i class="fas fa-angle-left"></i> Back to list</a>

    {{ include('package/_delete_form.html.twig') }}
{% endblock %}
