<?php

namespace App\Tests\Controller;

use PHPUnit\Framework\TestCase;

class PackageTest extends TestCase
{
    public function testPrivateAccess()
    {
        // Login as basic user

        // Create a new Package with public access

        // Create a new Package with private access

        // Assert owner user can see her private packages

        // Logout and assert anonymous user can see only public packages

        $this->assertTrue(true);
    }
}
